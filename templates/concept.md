# 概念名称

英文：The name of the concept

## 解释

这是对概念的阐释，您可以自由发挥。阐释应尽量详尽，易懂，并且我们建议您举个例子。

## 实践

在介绍诸如包管理器/常用命令一类的概念时，请您在这里列举出较为常见的指令。

在此以dnf作为例子。

1. `sudo dnf install xxx` 安装某个软件包
2. `sudo dnf remove xxx` 卸载某个软件包
3. `sudo dnf update` 更新所以软件包
4. ...